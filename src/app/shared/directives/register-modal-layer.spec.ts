import { RegisterModalLayer } from './register-modal-layer';

describe('RegisterModalLayer', () => {
  it('should create an instance', () => {
    const directive = new RegisterModalLayer();
    expect(directive).toBeTruthy();
  });
});
